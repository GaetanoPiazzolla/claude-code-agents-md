{
  "name": "agents-md",
  "version": "0.0.3",
  "description": "Claude Code plugin that loads agents.md as session context",
  "private": true,
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
